import { View } from "react-native"
import { HeaderBar } from "../../components/HeaderBar"

export const MainScreen = () => {
    return (
        <View>
            <HeaderBar/>
        </View>
    )
}